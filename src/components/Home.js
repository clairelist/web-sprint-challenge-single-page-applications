import React from 'react';

const Home = () => {
    return(
        <div className='home-wrapper'>
            <p>ORDER NOW! CONSUME ! CONSUME ! CONSUME !</p>
        </div>
    )
}

export default Home;